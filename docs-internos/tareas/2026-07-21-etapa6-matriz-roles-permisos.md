# Etapa 6 — Matriz de Roles y cierre de gaps Admin (21/07/2026, posterior a las Etapas 1-5 y a la auditoría de coherencia del mismo día)

Pedido: convertir la "Matriz de permisos" placeholder en la fuente REAL de roles/permisos, y cerrar los gaps declarados de los módulos Admin (Empleados, Catálogo Maestro, Empresas, Usuarios, Suscripciones) implementando SOLO lo que cada MD ya define. Commits en `develop`, no en `main`, uno por módulo. `frontend/public/sw.js` (cambio ajeno pre-existente) fuera de todos los commits. Sin SQL nuevo — esta etapa no necesitó ninguna migración.

### Módulo 1 — Matriz de Roles y Permisos real (no placeholder)

- **Nueva fuente única: `frontend/src/lib/matriz-permisos.ts`** — constante tipada `MATRIZ_PERMISOS` (22 filas = acciones concretas, agrupadas en 9 módulos: Acceso a productos, Cotizaciones, Facturas, Informes, Planes y pagos, Empresa, Administración de plataforma, Partner, Soporte) × 5 roles (`ROLES_MATRIZ`: admin/gerente/tecnico/partner/movil). Valores de celda: `si`/`no`/`condicional`/`noaplica`/`pendiente` — toda celda SIN regla de negocio cerrada quedó explícitamente `"pendiente"` (nunca un default silencioso). Helpers: `celdasPendientes()`, `permisoDe(id, rol)`, `modulosMatriz()`. Cada fila lleva un campo `gating` (NO renderizado en UI, cumple la regla de no citar arquitectura en pantalla) que documenta dónde vive HOY el enforcement real (archivo:función/política RLS) — la matriz y el gating existente quedan alineados por documentación cruzada, no divergentes.
- **UI real**: `AdminRolesView.tsx` pestaña "Permisos" ya no es decorativa — renderiza `MATRIZ_PERMISOS` dinámicamente (tabla por módulo, leyenda, KPIs de acciones/roles/celdas pendientes). Las notas de celda se redactaron sin nombres de `.md`, sin `§`, sin "Doc N" (regla permanente 20/07/2026).
- **Pestañas "Roles" y "Planes" verificadas sin contradicción**: "Roles" lista Admin/Gerente/Técnico (mismos roles de la matriz — Partner y Móvil-independiente no son "roles de cliente de Empresa" en esa pestaña, son columnas de la matriz por ser actores reales del ecosistema, coherente con que Partner no es producto); "Planes" lee `PRECIOS_OFICIALES`/`LIMITES_PLAN`, la misma fuente que la fila "Modificar el catálogo de planes" de la matriz (denegada para todos, catálogo de solo lectura).
- **Decisión sobre `MATRIZ_PERMISOS_APROBADA` (`roles-planes-oficial.ts:85`): se queda en `false` a propósito.** Ese flag representa la aprobación formal del propietario (Doc 12 §8) — activarlo sería inventar esa aprobación, y además la matriz contiene celdas ❓ sin decidir. Cambio de semántica documentado en el propio archivo: el flag ya NO oculta contenido (la matriz real se renderiza siempre); hoy solo controla el banner "aprobación formal pendiente" en Admin → Roles → Permisos. Comportamiento visible si Mohamed lo activa en el futuro: desaparece ese banner, nada más.
- **Alcance honesto del re-cableado**: NO se re-cablearon los puntos de gating existentes (middleware, layouts, RLS, `plan-limits.ts`) para leer la constante en runtime — son muchos, ya están probados en producción y re-cablearlos era demasiado invasivo para esta etapa. Fuente única declarativa = la matriz; el gating ya construido queda documentado en el campo `gating` de cada fila como reflejo de ella. `permisoDe()` queda disponible para conectar puntos de gating futuros.
- **Celdas ❓ pendientes de decisión de Mohamed (6)**: (1) ¿Admin puede usar Móvil? (2) ¿Partner puede usar Móvil? — hoy ninguna de las dos está bloqueada en código, solo exigen sesión; (3) Roles personalizados del Gerente — sistema construido pero inerte, decisión de negocio ya conocida, ahora visible en la matriz; (4) Mis Tarifas para Técnico — la arquitectura oficial dice que el Técnico no las administra, pero NO existe gating real en código (divergencia real detectada en esta etapa); (5) ¿Partner debe tener flujo de tickets de soporte? (hoy su panel no lo tiene).
- **Verificado**: `tsc --noEmit` limpio, `next lint` solo con los 3 warnings preexistentes de `useCotizacion.ts`/`useFactura.ts`, `next build` limpio (81 rutas). No verificado visualmente (regla del proyecto: verificación visual siempre en Vercel con la cuenta de Mohamed, nunca dev server local).

### Módulo 2 — Empleados Admin: Importar CSV (gaps del MD 07)

- **Infraestructura compartida nueva** (reutilizada por los demás módulos de esta etapa): `frontend/src/lib/csv-import.ts` (parser CSV 100% cliente — comillas/`""` escapado/CRLF/BOM, separador `,` o `;` autodetectado, `csvAObjetos()` por cabecera normalizada) y `frontend/src/components/admin/ImportarCsvBoton.tsx` (botón + file input + progreso + resumen de errores por fila; procesamiento SECUENCIAL a propósito porque cada fila puede disparar una invitación de correo real). Cada fila se procesa vía la Server Action ya existente del módulo — mismo guard `requireAdmin()`/`errorSiNoEsAdmin()` y misma validación que la creación manual; cero endpoints nuevos, cero tablas nuevas (los MD lo prohíben).
- **Empleados** (`AdminEmpleadosInternosView.tsx`): "Importar empleados (CSV)" en Acciones rápidas — columnas `nombre,email,cargo,departamento` (las mismas de la plantilla CSV ya existente del 13/07), cada fila llama `nuevoEmpleadoAction` (ficha en `darivo_admin_empleados` + invitación real de Supabase Auth). Exportar y plantilla ya existían (13/07/2026) — sin duplicar. Nota de alcance de la vista actualizada.
- **Alcance honesto "Excel/CSV"**: se acepta CSV (UTF-8); un `.xlsx` binario NO se parsea — la UI lo explica ("Desde Excel: Guardar como → CSV") y la plantilla descargable ya es CSV. Aplicar a los 4 módulos con importación de esta etapa.
- **NO construido, preguntas abiertas (MD 07)**: (a) "Publicar cambios" — el MD lo lista como botón pero no define ningún flujo borrador/publicado, y su §10 prohíbe crear tablas (no hay dónde persistir un estado borrador): ¿qué comportamiento exacto quiere Mohamed, o se elimina del diseño? (b) "Conectados ahora" (Trabajo en equipo) — el MD no exige de forma inequívoca infraestructura de presencia en tiempo real; NO se construyó (regla explícita de la tarea): ¿se quiere presencia real (infra nueva) o basta una aproximación tipo "activos en los últimos N minutos"? (c) Pestañas Invitaciones/Actividad/Historial de cambios siguen como placeholders honestos — requieren tabla de auditoría que el MD no autoriza.
- **Verificado**: `tsc` limpio, `lint` con solo los 3 warnings preexistentes, `next build` limpio (81 rutas).

### Módulo 3 — Catálogo Maestro: Importar categorías (gaps del MD-10)

- **"Importar categorías (CSV)"** (`AdminCatalogoView.tsx`, Acciones rápidas — exactamente donde el MD-10 §7 la ubica): columnas `nombre,sector,emoji`; el sector se resuelve por nombre o slug (insensible a tildes/mayúsculas) contra `catalogo_sectores` reales — si el sector no existe, la fila falla con error legible (nunca se inventa un sector). Sector Construcción → la fila se crea como subcategoría (mismo mecanismo del formulario manual: son categorías con `sector=construccion`, sin nivel nuevo en BD). Cada fila llama `crearCategoriaAction` (guard admin + validación existentes). "Descargar plantilla de importación (CSV)" actualizada a las mismas 3 columnas (antes tenía columnas que no correspondían a ningún import real).
- **"Publicar cambios" NO construido, pregunta abierta (MD-10)**: el MD lista el botón (§6) y "Cambios pendientes de publicación" (§7), pero NO define ningún flujo borrador/publicado, y §9 (v1.3) documenta el CRUD real actual como cambios DIRECTOS sobre las 4 tablas existentes, sin tablas nuevas. Construir borrador/publicado exigiría estado nuevo en BD (prohibido por el propio MD) y cambiaría el comportamiento ya autorizado el 18/07/2026. Pregunta para Mohamed: ¿el comportamiento definitivo es el actual (cambios directos) o quiere el flujo borrador→publicar (requiere decisión de esquema)? Mientras tanto la UI lo dice honestamente y la card KPI "Cambios pendientes" sigue en "—".
- **Verificado**: `tsc` limpio, `next build` limpio (81 rutas), lint sin warnings nuevos.

### Módulo 4 — Empresas: Importar CSV (gaps del MD 02)

- **"Importar empresas (CSV)"** (`AdminEmpresasView.tsx`, Acciones rápidas — ubicación exacta del MD 02 §7): columnas `razon_social,email_gerente,ruc,telefono` (las mismas de la plantilla ya existente); cada fila llama `crearEmpresaAction` — misma semántica que "+ Nueva empresa": invita al gerente por correo REAL, crea la fila en `empresas` y deja el perfil en plan Business (comportamiento ya autorizado de esa acción, sin cambios). La UI lo advierte antes de importar. Plantilla renombrada a "de importación" para dejar claro que sus columnas son las del import.
- **NO construido, preguntas abiertas (MD 02)**: (a) "Publicar cambios" — sin definición de flujo borrador/publicado en el MD y su §9 prohíbe tablas nuevas: misma pregunta que en Empleados/Catálogo; (b) **"Solicitar eliminación de empresa" — NO se inventó** (instrucción explícita de la tarea): requiere que Mohamed defina el procedimiento administrativo (quién solicita, quién aprueba, qué pasa con los datos/facturas de la empresa, plazo de reversibilidad); la UI ya lo decía honestamente desde antes y se mantiene; (c) pestañas Solicitudes/Historial siguen placeholders (sin tabla de auditoría autorizada); (d) KPIs "Autónomos"/"Inactivas" siguen no calculables (`empresas.activo` solo distingue 2 estados) — ya documentado en el propio código.
- **Verificado**: `tsc` limpio, `next build` limpio (81 rutas).

### Módulo 5 — Usuarios: Exportar/Importar CSV + invitación masiva (gaps del MD 03)

- **Nueva Server Action `invitarUsuarioAction(email)`** (`admin/usuarios/actions.ts`): invita a un usuario NUEVO por correo real de Supabase Auth (mismo flujo que un registro — `handle_new_user()` crea el perfil en plan gratis); guard `errorSiNoEsAdmin()` como el resto; error legible si el correo ya tiene cuenta confirmada. Compartida por Importar CSV e invitación masiva.
- **UI (`AdminUsuariosView.tsx`)**: botón "Exportar" (CSV de la vista filtrada — email/razón social/teléfono/plan/estado/método/onboarding/fechas) + panel desplegable "Importar / Invitación masiva" con: Importar usuarios (CSV, columna `email`, plantilla descargable) e "Enviar invitación masiva" (textarea, un correo por línea o separados por coma/`;`, deduplicados, envío secuencial con progreso y resumen de errores por correo). Nota al pie actualizada (antes decía "no están construidos todavía").
- **NO construido**: "Nuevo usuario" individual (MD 03 §5) sigue sin formulario propio — el import de 1 fila lo cubre funcionalmente, pero el botón dedicado no existe; se deja como pendiente menor (no estaba en el alcance pedido de esta tarea). Columnas "Empresa" y "Último contacto con soporte" del listado (§6) siguen sin mostrarse — ya eran gaps conocidos, sin cambios.
- **Verificado**: `tsc` limpio, `lint` con los 3 warnings preexistentes, `next build` limpio (81 rutas).

### Módulo 6 — Suscripciones: NO construido a propósito (contradicción MD 04 vs regla cerrada)

- `04-PANEL-ADMIN-SUSCRIPCIONES.md` §5 declara "Nuevo plan / Importar planes (Excel/CSV) / Publicar cambios" y §"Precio editable" dice que el Admin puede modificar precios en cualquier momento. **Eso contradice frontalmente la regla de negocio ya cerrada por el propietario** (misma tarea de esta etapa): catálogo de solo lectura, fijo a 3 planes (Básico S/49, Pro S/89, Business S/130), nunca inventar planes nuevos — y la fuente única real es `PRECIOS_OFICIALES`/`LIMITES_PLAN` en código, sin UI de edición. Siguiendo la instrucción explícita de la tarea, **no se construyó nada** en este módulo (cero cambios de código); `AdminSuscripcionesView` ya documentaba honestamente esas acciones como no aplicables. Pregunta para Mohamed: ¿actualiza el MD 04 (documento protegido — solo él puede) para reflejar el catálogo de solo lectura, o de verdad quiere planes editables desde Admin (lo que exigiría mover los planes de constante TS a BD, decisión de arquitectura + negocio)? La matriz de la Etapa 6 (Módulo 1) ya registra "Modificar el catálogo de planes" como denegado para todos, incluido Admin.

