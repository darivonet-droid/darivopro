# Actualización 21/07/2026 — ETAPA 4: resolución de las 10 preguntas abiertas (el propietario respondió "sí a todas las 10, opción por defecto/recomendada en cada una"; commits en `develop`, no en `main`; migraciones preparadas SIN ejecutar, como siempre):

- **#1 IMPLEMENTADO — fuente de verdad de Admin unificada: la tabla manda, la env es solo fallback de arranque.** `esAdministradorDarivo()` (`frontend/src/lib/acceso-producto.ts`) pasa a async: consulta `darivo_admin_empleados` (filas `activo=true`) con el cliente service_role — obligatorio porque la RLS de esa tabla solo la deja leer a quien YA es admin (`is_darivo_admin()`); con el cliente de sesión un no-admin vería siempre 0 filas, indistinguible de "tabla vacía". Reglas exactas: (a) si la tabla tiene ≥1 fila activa, es la ÚNICA fuente — un email en `DARIVO_ADMIN_EMAILS` sin fila activa YA NO es Admin; (b) si la tabla no tiene NINGUNA fila activa (arranque de entorno nuevo) o no se puede consultar (sin `SUPABASE_SERVICE_ROLE_KEY` en local), cae a la allowlist env, que sigue fallando cerrado si también está vacía. Latencia: cache en memoria de 60s del set completo de emails activos (tabla diminuta) — el peor caso es 1 consulta extra por instancia serverless cada 60s; una revocación tarda ≤60s + TTL de instancia en propagarse. Los 6 call sites actualizados (`middleware.ts:85`, `destino-post-login.ts:55`, `mora-pagos.ts:89`, `admin/empleados/actions.ts:15` `requireAdmin()`, y los 2 internos `errorSiNoEsAdmin()`/`verificarAccesoProducto()` — todos ya eran async, sin cambio de firma hacia afuera). **⚠️ Consecuencia operativa ANTES de desplegar a `main`**: este cambio es efectivo sin migración (la tabla existe desde el baseline) — si `darivo_admin_empleados` en producción tiene filas activas que NO incluyen alguna cuenta Admin real de la env, esa cuenta pierde acceso al desplegar. El propietario debe confirmar que sus cuentas Admin reales (ej. yatriye@gmail.com) tienen fila activa en la tabla (Admin → Empleados ya la gestiona desde el 13/07).
- **#5 IMPLEMENTADO — `ProductosEcosistemaLinks.tsx` eliminado** (código muerto): grep fresco previo en todo el repo confirmó 0 imports/usos reales. Nota: `.cursor/rules/05-FRONTEND-DARIVO-PRO.md:86` todavía lo menciona como componente de `/dashboard` (nunca fue verdad — jamás estuvo montado en ninguna página); MD protegido, se reporta sin tocar.
- **#8 IMPLEMENTADO — migración `20260721190000_perfiles_ruc_emisor_check.sql` (SIN ejecutar)**: `CHECK (ruc IS NULL OR ruc ~ '^(10|20)\d{9}$') NOT VALID` sobre `perfiles.ruc` — mismo patrón que el CHECK de comprador de la Etapa 3; nullable se mantiene (Móvil-independiente puede no tener RUC). Schema verificado en la cabecera del SQL (baseline:222-239, `ruc` línea 226; ningún ALTER posterior toca esa columna).
- **#6 PLANIFICADO (no implementado a propósito) + fase 0 hecha**: migrar `facturas.items` (jsonb) a tabla hija es refactor de riesgo alto sobre facturación real sin BD de prueba — NO se hizo de un golpe. Plan completo por fases en `frontend/docs-internos/TODO-PENDIENTES-DARIVO-PRO.md` §3-bis: **Fase 0 (hecha)** = migración `20260721200000_factura_items_tabla_preparada.sql` (SIN ejecutar) crea `factura_items` espejo de `cotizacion_items` (código `svc_id` + unidad `unit` por línea, FK a `facturas.inv_id` CASCADE, índice de FK, RLS idéntica al patrón del padre), **inerte: cero código conectado**; **Fase 1** = doble escritura desde `useFactura.crear()` (bloqueada por la pregunta #7 — el formato de código/serie que exija Bizlinks condiciona el mapeo); **Fase 2** = backfill idempotente del jsonb histórico; **Fase 3** = corte de lectura (PDF/fichas) con fallback al jsonb para no romper PDFs históricos; **Fase 4** = retiro de `facturas.items`. Condiciones para avanzar: respuesta a #7 + BD de prueba + una fase por despliegue.
- **#4 CONFIRMADO, sin cambio de código**: el modo solo-lectura por mora NO se extiende a `soporte_tickets`/`usuario_sectores`/`perfiles` — un usuario en mora debe poder contactar soporte y gestionar su perfil/pago para salir de la mora; bloquearlos sería contraproducente. La migración de mora (`20260721120000`) queda tal cual.
- **#9 CONFIRMADO, sin cambio de código**: factura rechazada por SUNAT → siempre comprobante NUEVO con correlativo nuevo, nunca se reutiliza/edita el rechazado. Ya coherente con lo construido: el trigger `facturas_proteger_campos_fiscales` (Etapa 3) congela `inv_num`/`inv_date`/`tipo_doc` en cuanto el estado sale de Borrador/En proceso, incluida `Rechazada`.
- **#10 CONFIRMADO, sin cambio de código**: FK `from_quote_id` se queda en `ON DELETE RESTRICT` tal como la dejó la Etapa 3 (no se pierde trazabilidad; borrar una cotización ya facturada da error de FK, comportamiento aceptado).
- **#2, #3, #7 — PENDIENTES REALES DEL PROPIETARIO (no resolubles desde el repo)**: (#2) confirmar en Vercel → Settings → Environment Variables (Production) que `DARIVO_ADMIN_EMAILS` no incluye darivonet@gmail.com — las env vars del panel de Vercel no son accesibles desde el repo; (#3) confirmar en el SQL Editor que darivonet@gmail.com no tiene fila activa: `SELECT * FROM public.darivo_admin_empleados WHERE lower(email) = 'darivonet@gmail.com' AND activo = true;` — requiere la BD real, sin credenciales en este entorno (y con #1 implementado, la tabla es ahora la fuente que de verdad decide); (#7) preguntar a Bizlinks (documentación de onboarding) cómo asigna series al registrar cada empresa emisora — sin ese dato no se puede diseñar series-por-emisor sin inventar; bloquea además la Fase 1 del plan de #6.
- **Migraciones pendientes de correr en el SQL Editor, EN ESTE ORDEN (ahora son 5)**: 1) `20260721120000_pago_fallido_solo_lectura.sql`, 2) `20260721160000_rls_etapa2_correcciones.sql`, 3) `20260721180000_facturas_bizlinks_esquema.sql`, 4) `20260721190000_perfiles_ruc_emisor_check.sql`, 5) `20260721200000_factura_items_tabla_preparada.sql` (las 2 nuevas no dependen técnicamente de las anteriores, pero el orden de timestamps evita divergencia repo↔BD).
- **Verificado (Etapa 4)**: `tsc --noEmit` limpio; `next lint` solo con los warnings preexistentes. Sin SQL ejecutado, sin tocar `main`, sin push. `frontend/public/sw.js` y el resto de cambios ajenos del working tree (sesión Latitude: `openai.ts`, `latitude.ts`, `package.json`, rutas de IA, bloque propio de CLAUDE.md) quedaron FUERA de todos los commits de esta etapa (un commit intermedio los arrastró por un staging ajeno concurrente y se rehízo limpio de inmediato — verificado con `git show --stat` commit por commit).

