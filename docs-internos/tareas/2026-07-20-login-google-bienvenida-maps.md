# Cambios 20/07/2026 (sesión aparte) — Login con Google, bienvenida y clic en dirección → Maps

Pedido por Mohamed el 20/07/2026: conectar credenciales reales de Google Cloud (Maps + OAuth), activar login con Google con el mismo correo de bienvenida que el registro normal, y hacer clicable la dirección del cliente en la lista de Clientes → Google Maps. Confirmado **"producción autónomo"** para esta tarea (main directo, sin pausas de Deploy).

**Investigación previa (obligatoria, Regla de oro) cambió el alcance real de 2 de los 4 puntos pedidos:**

1. **Autocompletado de Maps — ya estaba construido** (sesión anterior del mismo día, ver bloque "9." más abajo): `DireccionAutocomplete.tsx` ya conectado en Nueva Factura y Ficha de Cliente, leyendo `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY`. No se tocó código — solo falta la key real en Vercel (pendiente del propietario, ver abajo). "Nuevo cliente" no tiene campo Dirección — no se agregó (no pedido, `no inventar campos` explícito en el prompt).
2. **Login con Google — el botón/handler/callback ya existían** (`login/page.tsx`, `auth/callback/route.ts`, `signInWithOAuth`), de antes de esta sesión. Lo que sí se construyó: `auth/callback/route.ts` ahora dispara el email de Bienvenida (mismo `enviarBienvenida()`, plantilla y remitente `info@` — no `noreply@`/`noreplay@` como decía el pedido; ver hallazgo abajo) en el primer login real de una cuenta, detectado por `created_at`/`last_sign_in_at` a menos de 10s de diferencia (Supabase no expone un flag directo "es la primera vez" — interpretación propia, mismo patrón de umbrales ya usado en `cumpleFiltroFecha()`). Nuevo `lib/email/bienvenida-nueva-cuenta.ts`: extrae la lógica de nombre/plan/monto de `api/emails/bienvenida/route.ts` para compartirla entre ese endpoint y el callback, sin duplicarla; también usa `full_name`/`name` de Google como nombre si no hay `nombre_empresa` (antes solo caía al prefijo del correo).
   - **Hallazgo — remitente real es `info@darivopro.com`, no `noreply@`/`noreplay@`**: el pedido decía "mismo remitente `noreplay@darivopro.com`" (typo evidente de `noreply@`), pero el email de Bienvenida real siempre fue `info@` (`enviarBienvenida()`, `lib/email/send.ts`, ya así antes de esta sesión). No se cambió el remitente — cambiar una dirección de envío es una decisión de negocio no pedida explícitamente aparte del texto de este prompt (regla de "no inventar decisiones de negocio"); ambos métodos de registro ya comparten el mismo remitente, que es lo que pedía el requisito real ("no debe haber dos flujos distintos"). Si Mohamed quiere `noreply@` específicamente, es un cambio de una línea (`cuenta: "info"` → `"noreply"` en `enviarBienvenida()`).
3. **Clic en dirección → Maps — construido, solo en `/clientes` (Móvil)**: `ClientesView.tsx`, la dirección del cliente (ya se traía en la query, no se mostraba) ahora es un link a `google.com/maps/search/?api=1&query=<dirección>`, `target="_blank"`, con `stopPropagation` para no disparar la navegación a la ficha del cliente al hacer clic. **No aplicado a `/empresa/clientes`** (su query ni siquiera trae `direccion` hoy) — fuera del alcance literal pedido ("la pantalla de lista de Clientes"), documentado aquí como gap conocido en vez de inventar el mismo cambio ahí sin pedido explícito.
4. **Variables de entorno — no configuradas por el asistente**: no existe Vercel CLI ni token de API en este entorno, y la única alternativa (escribir las 3 credenciales en el formulario del Dashboard de Vercel vía navegador) está prohibida por política de seguridad del asistente, sin excepción por autorización del propietario. Mohamed las configura directamente.

**⚠️ Pendiente del propietario — bloquea la verificación de 3 de los 4 puntos:**
- `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET` en Vercel (Production + Preview) — nombres ya confirmados contra `.env.example`.
- `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` **también** en el Dashboard de Supabase (Authentication → Providers → Google, activar el proveedor) — **no van en Vercel para que el login funcione**, Supabase Auth hace el intercambio OAuth con Google desde su propio servidor, no la app Next.js. Activar un proveedor hoy apagado en producción es justo la excepción #3 de "Dos modos de despliegue" (arriba) — el asistente no lo activa por su cuenta aunque el resto de la tarea sea autónoma.

**Verificado**: `tsc --noEmit`, `next lint` (mismos 3 warnings preexistentes) y `next build` limpios (81 rutas). Push a `main` (commit `ad26184`, deploy `dpl_7Bp9xGyYjhjF2zTEkg4wx7o9tQk4` READY) — desplegado por "producción autónomo" de esta tarea.

**Verificado en vivo (evidencia dura, `darivopro.com`, sin sesión)**: clic en "Continuar con Google" devuelve `400 {"error_code":"validation_failed","msg":"Unsupported provider: provider is not enabled"}` desde `vyrtokggypcmpforglch.supabase.co/auth/v1/authorize?provider=google...` — confirma con evidencia real (no suposición) que el proveedor Google todavía no está activo en Supabase, tal como se documentó arriba como pendiente del propietario.

**No verificado, bloqueado por falta de sesión**: autocompletado de Maps (necesita la key real, pendiente) y el clic en dirección → Maps de `/clientes` (necesita iniciar sesión con datos reales de cliente; no hay cuenta a la que el asistente pueda entrar sin escribir una contraseña, regla permanente). Recomendado que Mohamed, tras configurar Vercel + Supabase: inicie sesión con Google desde `darivopro.com/login`, confirme que aterriza en `/dashboard`, y en `/clientes` haga clic en la dirección de un cliente con dato real para confirmar que abre Google Maps en pestaña nueva.

---

