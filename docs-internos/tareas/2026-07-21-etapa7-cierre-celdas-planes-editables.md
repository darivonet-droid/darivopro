# Etapa 7 — Cierre de las 6 celdas ❓ pendientes de la Etapa 6 + planes editables (21/07/2026, mismo día)

Pedido: implementar 7 decisiones ya resueltas por el propietario, la mayoría cerrando celdas `"pendiente"` de `matriz-permisos.ts` dejadas explícitamente abiertas en la Etapa 6. 7 commits en `develop` (uno por decisión), sin tocar `main`, sin push, sin ejecutar SQL. `frontend/public/sw.js` (cambio ajeno) fuera de todos los commits.

**1. Admin no puede usar Móvil — solo matriz, sin bloqueo real por dispositivo.** `acceder-movil.admin` pasó de `"pendiente"` a `"no"`. No se construyó enforcement real en `(auth)/layout.tsx` (fuera de alcance, instrucción explícita — es una etapa aparte).

**2. Partner puede usar Móvil, activado por Admin.** Nueva columna `partners.acceso_movil boolean NOT NULL DEFAULT false` (migración `20260721220000_partner_acceso_movil.sql`, sin ejecutar). Toggle en el panel de detalle de `admin/partners` (mismo patrón que Activar/Suspender, pero independiente — nunca se activa junto con "Activar partner"). Nueva función `updatePartnerAccesoMovil()` (`ecosystem-store.ts`) + Server Action `setPartnerAccesoMovilAction()` (guard `errorSiNoEsAdmin()`). `acceder-movil.partner` → `"condicional"`. Igual que el punto 1: solo persiste el flag, sin enforcement real en el login de Móvil todavía.

**3. Empresa: módulos activables reemplazan el rol fijo Técnico.** Confirmado que "Cliente" no necesita un 3er flag — módulo base sin restricción de rol en ningún punto de gating (`empresa_empleados` no tiene columna de permiso para Clientes). **Cambio de default real**: `EmpresaEmpleadosView.tsx` — el checkbox "Puede crear Facturas" del formulario de invitación nace en `true` (antes `false`, Tarea 2 del 17/07/2026). Es indispensable de todos modos porque `invitarEmpleadoAction` inserta el valor que le manda el formulario, no un literal hardcodeado — no hay otro caller. Migración `20260721221000_empresa_empleados_factura_default_true.sql` (`ALTER COLUMN factura_habilitada SET DEFAULT true`, sin ejecutar) alinea también el DEFAULT en BD. Informe sigue en `false` por defecto (opcional, el Gerente lo activa aparte). Nueva fila `acceder-clientes-empresa` en la matriz documentando el hallazgo.

**4. Bloqueo real de "Mis Tarifas" para Técnico (divergencia de la Etapa 6).** `MasTabs.tsx` recibe una nueva prop `ocultarTarifas` (calculada desde `obtenerContextoAcceso().rol==='tecnico'` en `(auth)/mas/page.tsx` y `empresa/mas/page.tsx`, este último no llamaba antes a `obtenerContextoAcceso()` y ahora sí) que quita la pestaña "Mis Tarifas" del selector — mismo patrón que "Mis planes"/"Informes". `mis-tarifas.tecnico` → `"no"`.

**5. Partner puede crear tickets de soporte — extendido el sistema real ya existente.** Confirmado que la RLS de `soporte_tickets` (`soporte_tickets_user`, `auth.uid() = user_id`) no tiene ninguna restricción de rol ni de plan — no había bloqueo real de arquitectura. Nueva sección "Soporte" (7ma) en `PartnerPanel.tsx` con `SoporteTicketsView` embebido (mismo componente/API de Móvil/Empresa, cero tablas/endpoints nuevos). `crear-ticket-soporte.partner` → `"si"`.

**6. `MATRIZ_PERMISOS_APROBADA` activado a `true`** (`roles-planes-oficial.ts`). Efecto visible único: desaparece el banner "aprobación formal pendiente" en Admin → Roles → Permisos (confirmado leyendo la condición `!MATRIZ_PERMISOS_APROBADA` en `AdminRolesView.tsx`). No cierra por sí solo ninguna celda `"pendiente"` restante (ej. Roles personalizados del Gerente, sigue como sistema construido pero inerte, decisión de negocio aparte).

**7. Suscripciones editables desde BD — con advertencia de alcance real explícita.** Investigado antes de crear nada: `public.planes_catalogo` **ya existía** en el baseline (`id, slug, nombre, precio_mensual, precio_anual, activo, limites jsonb, created_at`, RLS ya lista: SELECT para cualquier autenticado, escritura solo `is_darivo_admin()`) — sin ningún consumidor real hasta ahora, no se creó una tabla redundante. Migración `20260721222000_planes_catalogo_datos_oficiales.sql` (UPSERT por `slug`, sin ejecutar) puebla/corrige los 3 planes con los precios y límites reales vigentes (el `seed.sql` local tenía datos desactualizados — S/39/S/79, sin fila Business). Nuevo `lib/planes-catalogo.ts` (tipos + `listarPlanesCatalogo()`/`actualizarPlanCatalogo()`) y `admin/suscripciones/actions.ts` (`actualizarPlanCatalogoAction`, guard `errorSiNoEsAdmin()`, nunca expone creación — sin 4to plan). `AdminSuscripcionesView.tsx`: modal de edición (nombre/precio mensual/precio anual/activo/límites en JSON) desde tarjetas, tabla y panel de detalle.

**⚠️ Alcance real de la decisión 7 — léase antes de asumir control total:** `PRECIOS_OFICIALES`/`LIMITES_PLAN` (`roles-planes-oficial.ts`) siguen siendo la **única fuente real** que leen checkout (`api/pagos/checkout`), `/precios`, `lib/plan-limits.ts` y los correos. Editar un plan desde Admin → Suscripciones actualiza **solo** `planes_catalogo` — hoy es un cambio administrativo/visual, **sin ningún efecto** en el precio real cobrado ni en los límites de uso realmente aplicados. Recablear esos consumidores a leer de BD es una **fase 2 no construida en esta etapa** — advertencia repetida en la propia UI (banner rojo permanente en la pestaña Planes), en la migración SQL y en `04-PANEL-ADMIN-SUSCRIPCIONES.md` v1.10.

**Documentación actualizada** (bajo autorización explícita del propietario, protección de documentos): `01-VISION-DEL-PRODUCTO.md` v2.16 (§12), `04-PANEL-ADMIN-SUSCRIPCIONES.md` v1.10, `06-PANEL-ADMIN-PARTNERS.md` v1.7, `11-ROLES-PLANES-PERMISOS-EMPRESA.md` v1.3, `PANEL-PARTNER.md` v1.3 (fuera del pedido original de 4 MDs, pero necesario para que el documento del propio Panel Partner no quedara contradiciendo la sección "Soporte" recién construida).

**Verificado**: `tsc --noEmit` limpio y `next build` limpio (81 rutas, mismo conteo — ninguna ruta nueva) tras cada decisión; `next lint` con los mismos 3 warnings preexistentes de `useCotizacion.ts`/`useFactura.ts`, sin relación. No verificado visualmente (regla del proyecto: verificación visual siempre en Vercel con la cuenta de Mohamed, nunca dev server local). Ninguna migración SQL fue ejecutada — las 3 de esta etapa (`acceso_movil`, `factura_habilitada` default, `planes_catalogo` UPSERT) quedan pendientes en `supabase/migrations/`, a la espera de que el propietario las corra en el SQL Editor.

