# PENDIENTE — Bloque de la Etapa 5 para integrar en CLAUDE.md

**Documento interno temporal (NO público, misma verificación de seguridad que el resto de `docs-internos/`).**

## Por qué está diferido

Al cierre de la Etapa 5 de la auditoría de seguridad (21/07/2026), `CLAUDE.md` tiene **ediciones sin commitear de otra sesión concurrente** (integración de telemetría Latitude). Editarlo o comitearlo desde esta etapa arrastraría trabajo ajeno a medio hacer, así que la actualización de estado de la Etapa 5 se difiere: el bloque de abajo debe copiarse **tal cual** dentro de la sección `## ESTADO REAL DEL PROYECTO` de `CLAUDE.md` (a continuación del bloque "ETAPA 4"), **una vez la sesión de Latitude haya commiteado sus cambios**. Después de integrarlo, este archivo se puede eliminar.

---

## Bloque exacto a añadir a CLAUDE.md

*Actualización 21/07/2026 — ETAPA 5 (cierre de la auditoría de seguridad/roles/esquema; commits en `develop`, no en `main`; sin ejecutar SQL, como siempre):*

- **#3 EJECUTADO (entregable principal) — migración `20260721210000_darivo_admin_baja_darivonet.sql` (SIN ejecutar)**: desactiva (`activo=false`) la fila de `darivonet@gmail.com` en `darivo_admin_empleados`. Decisión confirmada por el propietario: el único Admin real es `yatriye@gmail.com`. Se eligió **desactivar en vez de DELETE** porque es el patrón coherente de la tabla (columna `activo boolean NOT NULL DEFAULT true`; `is_darivo_admin()` y `esAdministradorDarivo()` filtran ambos por `activo=true`; Admin → Empleados ya gestiona Activar/Desactivar como flujo normal) y conserva rastro auditable. Idempotente: `UPDATE ... WHERE lower(email)='darivonet@gmail.com' AND activo=true` — 0 filas afectadas si no existe, sin error. Schema verificado en la cabecera del SQL (`baseline_v2.sql:446-455`; único ALTER posterior es el ENABLE RLS de `baseline_v2.sql:787`, ninguno toca columnas). **Con el fix #1 de la Etapa 4 (la tabla es la fuente principal de Admin), correr esta migración corta el acceso de darivonet a `admin.darivopro.com` sin tocar más código.** Precaución operativa: confirmar que `yatriye@gmail.com` tiene fila activa en la tabla; si darivonet fuera la única fila activa, al desactivarla la tabla queda sin filas activas y el acceso cae al fallback env (que con #2 = solo yatriye; sin lockout en ningún caso).
- **#2 CERRADO como instrucción (no ejecutable desde el repo)**: en Vercel → Settings → Environment Variables → **Production**, `DARIVO_ADMIN_EMAILS` debe quedar únicamente con el valor exacto `yatriye@gmail.com`. Verificado 21/07/2026 que el repo no documenta ningún otro correo Admin (`frontend/.env.example` tiene `DARIVO_ADMIN_EMAILS=` vacía; grep exhaustivo de `darivonet@gmail.com`/`yatriye@gmail.com` en todo el repo → solo notas históricas de este CLAUDE.md, ninguna allowlist/config con emails).
- **#5 VERIFICADO**: `ProductosEcosistemaLinks.tsx` sigue eliminado (0 archivos, 0 referencias en `frontend/src`).
- **#8 VERIFICADO**: `20260721190000_perfiles_ruc_emisor_check.sql` existe y es correcto (CHECK `NOT VALID` sobre `perfiles.ruc`, regex `^(10|20)\d{9}$`, nullable a propósito, extracto de schema en cabecera).
- **#4, #9, #10**: siguen cerrados sin cambio de código (confirmados en la Etapa 4); documentación coherente, nada que actualizar.
- **#6/#7 EN PAUSA, sin ningún avance en esta etapa** (decisión del propietario): no se pregunta a Bizlinks todavía; la Fase 1 del plan de `factura_items` y cualquier migración adicional de facturación quedan congeladas. `TODO-PENDIENTES-DARIVO-PRO.md` §3/§3-bis actualizado para reflejarlo.
- **Migraciones pendientes de correr en el SQL Editor, EN ESTE ORDEN (ahora son 6)**: 1) `20260721120000_pago_fallido_solo_lectura.sql`, 2) `20260721160000_rls_etapa2_correcciones.sql`, 3) `20260721180000_facturas_bizlinks_esquema.sql`, 4) `20260721190000_perfiles_ruc_emisor_check.sql`, 5) `20260721200000_factura_items_tabla_preparada.sql`, 6) `20260721210000_darivo_admin_baja_darivonet.sql`.
- **Documentación**: `TODO-PENDIENTES-DARIVO-PRO.md` actualizado (nueva entrada de acciones #2/#3 en §1 con la lista de 6 migraciones; #7 marcado EN PAUSA en §3 y §3-bis). La actualización de CLAUDE.md de esta etapa se difirió (bloqueado por ediciones sin commitear de la sesión Latitude) — este mismo bloque quedó en `frontend/docs-internos/PENDIENTE-CLAUDE-MD-ETAPA5.md` hasta poder integrarse.
- **Verificado (Etapa 5)**: sin tocar ningún `.ts`/`.tsx` (solo SQL + MD); `tsc --noEmit` y `next lint` ejecutados solo como control (cualquier cambio ahí sería del working tree ajeno de Latitude, no de esta etapa). Sin SQL ejecutado, sin tocar `main`, sin push. Los archivos de la sesión Latitude (`CLAUDE.md`, `frontend/.env.example`, `frontend/package.json`, `frontend/package-lock.json`, `frontend/public/sw.js`, `frontend/src/app/api/darivo/chat/route.ts`, `frontend/src/app/api/ia/{cotizacion,gasto}/route.ts`, `frontend/src/lib/{openai,latitude}.ts`, `.claude/skills/`) quedaron fuera de todos los commits de esta etapa, intactos y sin commitear.
